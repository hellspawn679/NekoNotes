// Loader.js
import React from "react";

const Loader = () => {
  return (
    <div className="loader-container">
      ...
      {/* You can customize the loader further if needed */}
    </div>
  );
};

export default Loader;
